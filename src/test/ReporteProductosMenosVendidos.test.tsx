// --- Definición de Pruebas ---

describe('ReporteProductosMenosVendidos', () => {

  test('1. Debería mostrar el estado de carga y luego renderizar el reporte', async () => {

  });

  test('2. Debería aplicar la lógica de filtrado de órdenes (válidas) y agregación', async () => {

  });

  test('3. Debería ordenar los productos de forma ascendente (menos vendidos primero)', async () => {

  });
  
  test('4. Debería limitar la lista a solo los 10 productos menos vendidos', async () => {

  });

  test('5. Debería mostrar el mensaje de error o el mensaje de no datos', async () => {

  });

  test('6. Debería usar las props personalizadas y mostrar la fecha correctamente', async () => {
    
  });
});
