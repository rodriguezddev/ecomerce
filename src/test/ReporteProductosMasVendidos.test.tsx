// --- Definición de Pruebas ---

describe('ReporteProductosMasVendidos', () => {

  test('1. Debería mostrar el estado de carga y luego la tabla de productos', async () => {

  });

  test('2. Debería aplicar la lógica de filtrado (factura, pagado, no cancelado)', async () => {

  });

  test('3. Debería agregar correctamente las cantidades y ordenar los productos de forma descendente', async () => {

  });
  
  test('4. Debería limitar la lista a solo los 10 productos más vendidos', async () => {

  });

  test('5. Debería mostrar el mensaje de error o el mensaje de no datos', async () => {

  });

  test('6. Debería usar las props personalizadas y mostrar la fecha correcta', async () => {

  });
});
