// --- Definición de Pruebas ---

describe('AboutPage', () => {

  test('1. Debería renderizar correctamente el Header del componente', async () => {
    // Verifica que el componente Header se renderice correctamente
  });

  test('2. Debería mostrar el título "Nuestra Historia" correctamente formateado', async () => {
    // Valida que el título principal se muestre con los estilos adecuados
  });

  test('3. Debería renderizar los tres párrafos de la historia de la empresa', async () => {
    // Confirma que los tres párrafos descriptivos estén presentes
  });
  
  test('4. Debería aplicar los estilos y clases de Tailwind CSS correctamente', async () => {
    // Verifica que las clases de estilo se apliquen apropiadamente
  });

  test('5. Debería tener una estructura responsive para diferentes dispositivos', async () => {
    // Valida que el diseño sea responsive en móviles y desktop
  });

  test('6. Debería mostrar la información histórica de la empresa M&C7 de manera ordenada', async () => {
    // Confirma que la información histórica se presente secuencialmente
  });
});