// --- Definición de Pruebas ---

describe('ReporteProductosSinStock', () => {

  test('1. Debería mostrar el estado de carga y luego el reporte', async () => {

  });

  test('2. Debería aplicar el filtro de stock === 0 correctamente', async () => {

  });

  test('3. Debería mostrar el mensaje de "No hay productos sin stock" cuando la lista está vacía', async () => {

  });

  test('4. Debería mostrar el mensaje de error de la API', async () => {

  });

  test('5. Debería usar las props personalizadas y mostrar la fecha correctamente', async () => {

  });
});
